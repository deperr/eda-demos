---
- name: Site Down
  hosts: all
  gather_facts: false
  become: true

  tasks:
    - debug:
        msg: "Website is down!"

    - name: Start web service
      ansible.builtin.service:
        name: httpd
        state: started
        enabled: true