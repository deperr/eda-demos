---
- name: Automatic EDA Verification
  hosts: localhost
  sources:
    - ansible.eda.range:
        limit: 5
        delay: 1

  rules:
    - name: Catch range event and log message
      condition: event.i >= 0
      action:
        debug:
          msg: "EDA is working. Processing event number {{ event.i }}"
  
