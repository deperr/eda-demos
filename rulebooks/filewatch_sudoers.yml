---

- name: Monitor sudoers file for modifications
  hosts: all
  sources:
    - ansible.eda.file_watch:
        paths:
          - /etc/sudoers
        recursive: false

  rules:
    - name: Sudoers file was modified
      condition: event.file_watch.status == "modified"
      actions:
        - debug:
            msg: "ALERT: /etc/sudoers was modified on {{ event.meta.hosts }}"
        - run_job_template:
            name: "EDA | Remediate Sudoers Change"
            organization: "Default"
